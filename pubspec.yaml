name: flutter_video_gallery
description: Flutter Video Gallery app

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.21.0
  path: ^1.6.2
  uuid: 2.0.0

  path_provider: ^0.5.0+1
  camera: ^0.4.0+2
  video_player: ^0.10.0+2
  flutter_ffmpeg: ^0.1.1
  flutter_range_slider: ^1.1.0
  share_extend: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true